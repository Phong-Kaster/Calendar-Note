package com.example.skeleton.ui.theme

import androidx.compose.ui.graphics.Color

val ColorBlue = Color(0xFF2F70BC)
val ColorBlue2 = Color(0xFF35A0F5)

/*
 * --- The app's one and only dark palette ---
 * The app is always dark, so every colour role of the Material colour scheme is written here once.
 * Each "on" colour is the colour of text/icons drawn ON TOP of its base colour, so it is always
 * picked to stand out strongly against that base (light on dark, or dark on light).
 * Screens never use these vals directly — they read MaterialTheme.colorScheme.xxx instead.
 */

// ---------- Primary (brand blue) ----------
val DarkPrimary = Color(0xFF9ECAFF)
val DarkOnPrimary = Color(0xFF003258)
val DarkPrimaryContainer = Color(0xFF00497D)
val DarkOnPrimaryContainer = Color(0xFFD1E4FF)

// ---------- Secondary ----------
val DarkSecondary = Color(0xFFBBC7DB)
val DarkOnSecondary = Color(0xFF253140)
val DarkSecondaryContainer = Color(0xFF3B4858)
val DarkOnSecondaryContainer = Color(0xFFD7E3F7)

// ---------- Tertiary ----------
val DarkTertiary = Color(0xFFD6BEE4)
val DarkOnTertiary = Color(0xFF3B2948)
val DarkTertiaryContainer = Color(0xFF523F5F)
val DarkOnTertiaryContainer = Color(0xFFF2DAFF)

// ---------- Background & surfaces (near-black) ----------
val DarkBackground = Color(0xFF0B0D10)
val DarkOnBackground = Color(0xFFE2E2E6)
val DarkSurface = Color(0xFF0B0D10)
val DarkOnSurface = Color(0xFFE2E2E6)
val DarkSurfaceVariant = Color(0xFF43474E)
val DarkOnSurfaceVariant = Color(0xFFC3C7CF)
val DarkSurfaceTint = Color(0xFF9ECAFF)
val DarkInverseSurface = Color(0xFFE2E2E6)
val DarkInverseOnSurface = Color(0xFF2F3033)
val DarkInversePrimary = Color(0xFF0061A4)

// ---------- Surface levels (from lowest to highest) ----------
val DarkSurfaceDim = Color(0xFF0B0D10)
val DarkSurfaceBright = Color(0xFF34373B)
val DarkSurfaceContainerLowest = Color(0xFF060709)
val DarkSurfaceContainerLow = Color(0xFF15171B)
val DarkSurfaceContainer = Color(0xFF1A1C20)
val DarkSurfaceContainerHigh = Color(0xFF24272B)
val DarkSurfaceContainerHighest = Color(0xFF2F3236)

// ---------- Outlines & scrim ----------
val DarkOutline = Color(0xFF8D9199)
val DarkOutlineVariant = Color(0xFF43474E)
val DarkScrim = Color(0xFF000000)

// ---------- Error ----------
val DarkError = Color(0xFFFFB4AB)
val DarkOnError = Color(0xFF690005)
val DarkErrorContainer = Color(0xFF93000A)
val DarkOnErrorContainer = Color(0xFFFFDAD6)
