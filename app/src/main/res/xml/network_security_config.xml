<?xml version="1.0" encoding="utf-8"?>
<!--
    debug-overrides apply only when the app is debuggable (debug builds).
    Trusting user-installed CAs fixes TLS when a proxy/antivirus/dev tool
    re-signs HTTPS with a CA you installed on the device.
    Release builds still use system CAs only.
-->
<network-security-config>
    <!-- Debug build uses HTTP to jsonplaceholder; release stays HTTPS-only for this host. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">jsonplaceholder.typicode.com</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>