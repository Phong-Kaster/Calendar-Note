# T-008 — Add a note to the selected day; the repository refuses future-dated notes

- **Status:** ✅ complete
- **Depends on:** T-007, T-003
- **Attempts:** 1 / 3

## Description

The PRD's one explicit business rule, enforced where a second caller cannot walk past it.

> **The rule itself already landed, in T-003 (amendment A-006).** `NoteRepositoryImpl.save` refuses
> any note dated after `LocalDate.now(clock)` and returns `Outcome.Error`, on every write path
> including edit, with today inclusive — and **DoD criterion 10's test exists and was
> mutation-checked**. The reason it moved: `DOMAIN.md` puts the enforcement point in the repository
> so no caller can bypass it, and T-003 is where the first write path exists, so leaving `save`
> permissive would have been checkpointing code known to contradict a human-owned rule.
>
> **This task is now the second caller reaching that guard, plus the calendar half.** Do not
> re-implement the rule. Do re-verify it from this caller — a second route to the same guard is
> worth its own test, and this is the route that makes a refusal reachable by ordinary use.
> Read `.ai/TASKS/T-003.md` first.

- The Calendar screen offers an add-note action for the selected day, which navigates to the Note
  screen with **that day's** date (not today) through the existing `toNote` route from T-003.
- No add affordance is offered when the selection is a future day — which cannot happen, since T-006
  made future cells inert, but the action must not assume it.
- **`NoteRepository` refuses to save a note whose date is after today** and reports the refusal to
  its caller without throwing (`Outcome.Error`, per `.claude/repository-layer.md`: repositories never
  throw across the boundary). The rule lives in the repository, not in the calendar cell — a rule
  enforced only in a grid cell is a rule the next caller walks past.
- **This rule is now in `knowledge/DOMAIN.md`** (created by the human through escalation D-001), so
  it outranks the codebase: any difference is a defect in the code, never a reason to amend the rule.
  Implement it exactly as written there, including the parts this task summarises loosely:
  - it applies to **every write path — create and edit alike**. The check belongs on the single
    `save` the repository exposes, so both routes pass through it; if create and edit ever take
    different paths, both get the check.
  - the boundary is **inclusive of today**: today allowed, tomorrow refused.
  - the refusal is a returned value, never a thrown exception.
- **The refusal is surfaced to the user rather than swallowed, and this part is genuinely unfinished
  work for this task.** Today the editor shows the same `we_are_sorry` toast ("something went wrong,
  please try again") for a refusal as for a disk failure — and retrying a refusal can never succeed,
  while the editor offers no date control to change what was refused. That is survivable while the
  only reachable refusal needs the device clock to move backwards; it stops being survivable here,
  because this task is what lets a user pick the day. Distinguish refusal from failure in the
  outcome and give the refusal its own message naming the day. Filed as
  `knowledge/ISSUES.md` § *Leaving the Note editor either loses the user's text or traps them on the
  screen*, part 2 — resolve it here and delete that part of the entry.
- "After today" is evaluated against the device's local date. State the timezone assumption in the
  KDoc.

## Acceptance

Maps to DoD criteria 10 and the second half of 11.

- Build, unit tests and lint succeed.
- ~~**The rule test, which must never depend on a screen:**~~ **already satisfied in T-003** —
  `NoteRepositoryImplTest` proves tomorrow refused, far-future refused, edit-into-future refused,
  today accepted, yesterday accepted, and the row absent after a refusal; disabling the guard fails
  three of them. **Re-run it, cite it, and do not weaken it.** If this task changes `save` at all,
  mutation-check the guard again.
- A unit test proves a note created while day `D` is selected carries date `D`, not today. **This is
  the new rule-related work**: the seam exists (`openNote(noteId, date)` keeps whatever day it is
  given, tested in `NoteViewModelTest`) but nothing yet proves the *calendar* passes the selected
  day rather than today.
- A unit test proves the refusal is reported to the user as a refusal, distinguishable from a write
  failure.
- Recorded as **needing human inspection** (class (I)): adding a note from a selected past day shows
  it under that day, and the refusal path is visible if it is ever reachable.

## Evidence

All four commands were run on the final tree, after the review fixes and after every mutation had
been reverted (`grep -r MUTATION app/src` → no matches).

```
./gradlew :app:assembleDebug :app:testDebugUnitTest --rerun-tasks :app:lintDebug \
          :app:validateDebugScreenshotTest
BUILD SUCCESSFUL in 43s — 61 actionable tasks: 61 executed
```

| Command | Result | Where the number comes from |
|---|---|---|
| `:app:assembleDebug` | `BUILD SUCCESSFUL` | console |
| `:app:testDebugUnitTest --rerun-tasks` | **138 tests, 0 failures** (129 before) | `app/build/test-results/testDebugUnitTest/TEST-*.xml` — the console prints nothing when everything passes |
| `:app:lintDebug` | **0 errors, 64 warnings** — unchanged from T-007 | `app/build/reports/lint-results-debug.xml`, counted by severity |
| `:app:validateDebugScreenshotTest` | **20 cases, 0 failures** (19 before) | `app/build/test-results/validateDebugScreenshotTest/TEST-preview-screenshot-test-engine.xml` |

**The first run of that command set failed, and the failure was the expected one**: four screenshot
cases reported `ScreenshotImageNotFoundException` because three previews had grown to fit the new
add-note row and one was new, and a reference filename hashes the preview's parameters. It also
re-demonstrated the trap in `knowledge/PROJECT.md`: `validateDebugScreenshotTest` failing aborted
`testDebugUnitTest` and `lintDebug`, which never ran at all.

### The nine new unit tests

- `CalendarViewModelTest` (+4, now 41): the selected day is what a new note is dated to; picking
  again moves it; nothing-picked falls back to the clock; a day the clock has overtaken is handed
  over unchanged rather than swapped for today.
- `NoteViewModelTest` (+3, now 33): a refusal is reported as a refusal naming the day and **not**
  as a failure; the refusal clears once shown; Save works again after a refusal.
- `NoteRepositoryImplTest` (+2, now 41): a refusal carries the typed tag with both dates on it; a
  write the database refuses is **not** dressed up as a calendar refusal.

### Mutation checks — eleven, each predicted before it was run

Every mutation carried a comment containing the literal word `MUTATION` and was reverted
immediately; the tree was grepped clean before checkpointing.

| # | Mutation | Predicted | Observed |
|---|---|---|---|
| M1 | `dateForNewNote()` ignores the selection, always today | the 3 selection tests | exactly those 3 — and *not* the nothing-picked test, which declines to claim anything about a selection |
| M2 | falls back to `uiState.today` instead of the clock | the nothing-picked test only | 1, as predicted |
| M3 | quietly swaps an overtaken picked day for today | the handed-over-as-it-is test only | 1, as predicted |
| M4 | the refusal loses its type tag | the repository's tag test only | 1, as predicted — **see the finding below** |
| M5 | *every* save error tagged as a refusal | the not-dressed-up test only | 1, as predicted |
| M6 | the ViewModel never recognises a refusal (pre-T-008 behaviour) | the 2 refusal-reporting tests | exactly those 2 |
| M7 | `saving` left shut on the refusal path | "save works again after a refusal" | 1, as predicted |
| M8 | **the future-date guard removed entirely** | T-003's 3 rule tests + the new tag test | exactly those 4 — the rule is still defended from this task's side |
| M9 | the boundary off by one (`!isBefore` — today refused) | "a note dated today is accepted", plus anything that saves a note dated today | **6**, which is the boundary being defended more heavily than predicted: five other tests save a today-dated note incidentally, and the not-dressed-up test fails too because a today-dated note is now refused by the rule before it ever reaches the broken DAO |
| M10 | the add action drawn even with no day picked | no unit test; the `DayNotesNothingPicked` image | **all 138 unit tests stayed green** and 4 images failed — only `DayNotesNothingPicked` for the right reason. That case is the sole evidence for the conditional affordance |
| M11 | the refusal names `uiState.date` instead of the store's `refusal.date` | the refusal-naming test | 1 — **and this mutation passed before the review fix**, see finding 5 below |

**M4 is the one to carry forward: the entire user-visible improvement in this task rests on a
single repository test.** Every `NoteViewModel` refusal test builds its own tagged `Outcome.Error`
through the fake, so the fake cannot say whether the *real* store tags anything. Untag
`NoteRepositoryImpl.save` and 137 of 138 tests still pass while every refusal in the running app
goes back to "something went wrong, please try again". A fake that can express a contract does not
verify that the implementation honours it — only the test against the implementation does.

### Fresh-context review

One reviewer, fresh context, given the diff and the authority order. **12 findings: 0 Critical, 6
Major, 6 Minor. Nine fixed here, two accepted with reasons, one refuted.**

Fixed:

1. **Major — the add action had no double-navigation guard.** `onOpenNote` had one and
   `CoreBottomBar` debounces the centre button, but the new action had neither, and `toNote` is a
   global action with no `launchSingleTop`. Two taps would push two blank editors for the same day:
   the user types in the top one, saves, and lands on the second — indistinguishable from a save
   that did nothing. Fixed by collapsing all three navigation routes off this screen into one
   guarded `CalendarFragment.openNoteEditor(...)`, which also removed the duplicated guard.
2. **Major — `knowledge/ISSUES.md` part 2 was not deleted.** The code resolved it; the entry still
   said it was open, and a file read at Orient every iteration was therefore lying. Deleted, with a
   note saying what happened to it; part 1 (text loss on the way out) is untouched and still open.
3. **Major — the reference-image bookkeeping.** The four live PNGs are staged in this checkpoint;
   the three tracked images the height changes orphaned are removed from the index with
   `git rm --cached`; `ISSUES.md`'s stray list now names all eight strays rather than three.
4. **Major — `CoreBottomBar`'s KDoc asserted the opposite of the new behaviour** ("the answer for
   all three current screens is the same"). Rewritten to say the meaning is per-screen. See the
   A11 note below for why the behaviour change itself stands.
5. **Major (test quality) — a dead assertion.** Both refusal tests opened the editor on the same
   day the refusal named, so `refusal.date` and `uiState.date` were interchangeable and the
   ViewModel's documented choice between them had no test behind it. The fixture now uses two
   different days; M11 above is that fix being verified rather than asserted.
6. **Major (evidence gap) — `NoteFragment`'s toast wiring is uncovered by anything.** Recorded as
   a human-inspection item below rather than left implied.
7. **Minor — README's package tree** did not list the new `domain/model` file (criterion 14).
8. **Minor — the exception captured a stack trace** it would never use. Now constructed with
   `writableStackTrace = false`.
9. **Minor — two different plus glyphs on one screen.** The add row used `Icons.Filled.Add` while
   `CoreBottomBar`'s centre button uses `Icons.Rounded.Add`, and on this screen the two controls
   now mean the same thing and are visible together. Changed to `Rounded`, and the three affected
   images re-recorded (same parameters, so overwritten in place — no new strays).
10. **Minor — `CalendarFragment` had no class KDoc or `@author`.** Pre-existing, but the rule is
    always-on and this diff is the largest edit the file has had. Added.
11. **Minor — comment length.** `CLAUDE.md` wants thorough documentation and
    `.claude/jetpack-compose-ui.md` wants KDoc a person reads in two seconds; a 27-line KDoc over a
    one-line body is on the wrong side of that. `dateForNewNote`'s KDoc and `onCreateNote`'s
    comment were cut to their contract plus the two decisions, with the argument moved here.

Accepted with reasons:

12. **Minor — the add affordance is gated on "a day is picked", not on "the picked day is not in
    the future".** Criterion 9's wording is about the affordance, and the gate holds relative to
    `uiState.today` — `selectDate` refuses a later day and `refreshToday` drops one the clock
    overtook. It can be stale relative to the *real* clock for the window between a backwards clock
    step and the next resume, which is the window this task's whole refusal chain exists for.
    Re-checking the rule in the component would put a second copy of `DOMAIN.md` rule 1 above the
    repository, which is precisely what the rule forbids. The reviewer reached the same conclusion
    and reported it for the record; it is recorded.
13. **Minor — where `FutureDateRefusedException` lives.** `domain/model/` rather than beside
    `NoteProblem` in the UI layer, because the repository interface names it in its contract and
    `domain/` is the only package both layers may depend on.

Refuted, and worth keeping because the refutation is now a tool:

14. The review believed the tracked `BottomBarTabGermanLabel_…_967f30ea_0.png` was a dead file
    whose live counterpart was untracked — which would have meant `validateDebugScreenshotTest`
    passing on this host and failing on a clean clone, i.e. criterion 13's evidence not being
    reproducible from the checkpoint. It is not dead. The validation report names the file it used,
    and `967f30ea` is in that list. The reviewer reasoned from the tracked/untracked split, which
    cannot distinguish the two; the report can, in one `grep`. Added to
    `knowledge/PROJECT.md` § the orphan entry.

## Human inspection needed (class (I))

Five items, three of them new:

1. **The refusal message itself, and the day it names.** `NoteFragment`'s `LaunchedEffect` on
   `saveRefusedDate` is the entire user-visible half of this task and **no test in this project
   reaches it** — a Fragment needs a device here. Four mutations survive the whole suite: deleting
   the effect (a refusal then gives *no* feedback at all, worse than the toast it replaced), using
   `we_are_sorry` again (the defect restored), dropping `consumeSaveRefused()` (fires once, then
   never), and keying the effect on `Unit`. Judgement needed on the wording too: "11 September 2026
   has not arrived yet — a note can only be written on today or an earlier day" is long for a toast.
2. **Adding a note from a selected past day, end to end** — the task's own acceptance item. Pick 3
   September on the Calendar, tap "Add a note to this day", save, and check the note appears under
   the 3rd and carries that date on Home. The unit tests prove every link of that chain
   individually; only a device proves the chain.
3. **The add row reading as an action rather than as a third note.** It is deliberately shaped like
   `NoteSummaryRow` — same radius, border and surface — and set apart only by `primary` colour and
   a plus. `DayNotesWithNotes` is the image to judge it on.
4. **The centre bottom-bar button meaning the picked day on this screen** (see the A11 note below).
   A product call, not a machine-checkable one.
5. **Four reference images**, new or re-recorded and never seen by a person:
   `DayNotesEmptyDay` (260dp), `DayNotesGermanHeading` (280dp), `DayNotesWithNotes` (400dp),
   `DayNotesNothingPicked` (new).

## Notes

- This is DoD criterion 10 **and** `knowledge/DOMAIN.md` rule 1 — the one criterion the DoD singles
  out as machine-provable and required to stay that way. If the rule ends up somewhere a unit test
  cannot reach, the task is not done — move the rule, do not weaken the evidence.
- If you come to believe the rule as written is wrong or incomplete, that is an Escalation Request,
  never an edit to `DOMAIN.md`. The file is deny-listed for the engine.
- Off-by-one risk: "cannot create a note for a future date" means today is allowed. A `>=` where a
  `>` belongs makes today's notes impossible and every other test still passes. **M9 above says
  this is now defended six ways** — the boundary test plus five tests that save a today-dated note
  in passing.

### Decisions this task made, and why

**1. The centre bottom-bar button means the *picked* day on the Calendar screen, and today
everywhere else.** STATE.md left this open deliberately. The alternative — leaving it as today
here too — puts two add affordances a few centimetres apart on one screen, filing notes on two
different days, with nothing on either one saying so; a user who picked the 3rd, tapped the big
button and got a note dated today would not find out until they went looking. Two controls that
agree can be predicted. The cost is that the button is no longer literally identical on every
screen, and that is the smaller surprise on the one screen whose entire purpose is choosing a day.

DoD assumption **A11 was checked before doing this, and it does not bind**: its row is titled
"Create-note entry point **from Home** (PRD §3.1, sub-question)" and it resolves whether Home
should have one. Criterion 5 is Home-scoped in its own words ("Saving it returns to Home") and is
untouched — on Home and Settings the button still means today. Had A11 been unscoped this would
have been a Tier-3 proposal, not an implementation choice.

**2. A picked day the clock has overtaken is handed to the store as it is.** Three things could
happen to such a day and two are worse. Swapping it for today files a note on a day nobody chose,
silently. Refusing it in the ViewModel or the component writes `DOMAIN.md` rule 1 a second time,
above the repository, which is the one place the rule says it must not live — and a rule in two
places is a rule that will disagree with itself. So it travels on, `save` refuses it, and the
editor names the day. **That chain is why part 2 of the `ISSUES.md` entry had to be fixed here and
not deferred:** this task is what makes a refusal reachable without a clock change, and a refusal
that says "please try again" is an instruction that can never work.

**3. The refusal is tagged by type, not by message.** `Outcome.Error` carries `message` and
`throwable`; `throwable` is its only typed channel, so a never-thrown
`FutureDateRefusedException` travels in it. `DOMAIN.md` requires the refusal to be reported "as a
value (`common.Outcome`)" and the DoD's evidence row for criterion 10 says "returns an error
outcome", so widening `Outcome` with a fourth case or moving the refusal into `Outcome.Success`
were both ruled out — the second is tempting, because `getNote` already distinguishes "it is gone"
from "I could not look", but it contradicts the approved evidence row. Matching on message text was
the other option and it breaks the first time somebody rephrases a log line.

**4. The add action lives inside `CalendarDayNotes`, past the `dayLabel == null` return.** That is
what makes "no add action without a day to name it" hold by construction rather than by a caller
remembering to pass a flag. The cost was three previews growing to fit the row, and three orphaned
reference images — paid knowingly, and listed in `knowledge/ISSUES.md` for the human who can
delete files.
