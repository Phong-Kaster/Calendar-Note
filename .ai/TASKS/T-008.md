# T-008 — Add a note to the selected day; the repository refuses future-dated notes

- **Status:** pending
- **Depends on:** T-007, T-003
- **Attempts:** 0 / 3

## Description

The PRD's one explicit business rule, enforced where a second caller cannot walk past it.

- The Calendar screen offers an add-note action for the selected day, which navigates to the Note
  screen with **that day's** date (not today) through the existing `toNote` route from T-003.
- No add affordance is offered when the selection is a future day — which cannot happen, since T-006
  made future cells inert, but the action must not assume it.
- **`NoteRepository` refuses to save a note whose date is after today** and reports the refusal to
  its caller without throwing (`Outcome.Error`, per `.claude/repository-layer.md`: repositories never
  throw across the boundary). The rule lives in the repository, not in the calendar cell — a rule
  enforced only in a grid cell is a rule the next caller walks past.
- The refusal is surfaced to the user rather than swallowed. A save that silently does nothing is
  worse than a refusal that says why.
- "After today" is evaluated against the device's local date. State the timezone assumption in the
  KDoc.

## Acceptance

Maps to DoD criteria 10 and the second half of 11.

- Build, unit tests and lint succeed.
- **The rule test, which must never depend on a screen:** a JVM unit test with a fake DAO proves
  `save` refuses a note dated tomorrow and returns an error outcome, and accepts a note dated today
  and a note dated yesterday. Include the boundary explicitly — today must be *accepted*, tomorrow
  *refused*.
- A unit test proves a note created while day `D` is selected carries date `D`, not today.
- Recorded as **needing human inspection** (class (I)): adding a note from a selected past day shows
  it under that day, and the refusal path is visible if it is ever reachable.

## Evidence

<!-- Filled on completion: command + observed output for build / test / lint / review. -->

## Notes

- This is DoD criterion 10, the one criterion the DoD singles out as machine-provable and required to
  stay that way. If the rule ends up somewhere a unit test cannot reach, the task is not done — move
  the rule, do not weaken the evidence.
- Off-by-one risk: "cannot create a note for a future date" means today is allowed. A `>=` where a
  `>` belongs makes today's notes impossible and every other test still passes.
