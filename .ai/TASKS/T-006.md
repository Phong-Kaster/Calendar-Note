# T-006 — Calendar screen: month grid, today marked, month navigation, future days inert

- **Status:** ✅ complete — iteration 8
- **Depends on:** T-002, T-010
- **Attempts:** 1 / 3 (across three invocations — two died mid-flight; see *Evidence*)

## Description

Add the Calendar screen and get the month grid right before hanging any note behaviour off it.

- New screen `ui/fragment/calendar/` — `CalendarFragment`, `CalendarUiState`, `CalendarViewModel`,
  private `CalendarLayout` + `@Preview`s for the meaningful states. Reachable from the app's
  navigation: add the destination and action to `res/navigation/navigation_graph.xml` and an entry
  point (extend `BottomBarDestination` / `CoreBottomBar`, matching how Home and Setting are wired).
- Month grid for the displayed month, with weekday headers and correct leading/trailing blanks.
  Previous / next month controls.
- **The day-cell state is plain Kotlin, not a composable.** Put the mapping
  `(date, today, displayedMonth) -> DayCellState` (`Past`, `Today`, `Future`, `OutsideMonth`) in a
  testable place — `CalendarUiState` as a derived value, or a small domain helper. A rule that lives
  inside `@Composable` can only be checked by rendering it, and this repository cannot render in a
  test.
- Every day after today renders visibly disabled and ignores taps: no `clickable`, or a `clickable`
  whose `enabled` is false — not a click handler that silently does nothing, which is
  indistinguishable from a bug.
- Today is marked. Selection is marked. Check the **combined** states, not each alone: today *and*
  selected, selected *and* has-notes. A marker that repeats the fill it sits on is invisible
  (`POLICIES.md` § User-Interface Defects).
- All colours from the theme built in T-001.

## Acceptance

Maps to DoD criteria 8 and 9.

- Build, unit tests and lint succeed.
- A JVM unit test proves the day-cell state mapping: a date after today maps to `Future`, today maps
  to `Today`, a date before today maps to `Past`, and a date outside the displayed month maps to
  `OutsideMonth`. Include a month boundary (e.g. the 1st and the last day) and a leap-year February.
- A unit test proves the grid for a known month has the right number of leading blanks and the right
  day count.
- The diff shows no tap handler reachable for a `Future` cell.
- **Screenshot evidence** (harness from T-010): reference images for today+hasNotes,
  today+selected, selected+hasNotes, plain+hasNotes, a future/disabled cell, and the full month grid.
  `:app:validateDebugScreenshotTest` passes. The human approves these images once — that a disabled
  day is *legibly* disabled and that the markers are distinguishable from each other and from the
  fill underneath is judged by eye on first sight, and defended by the machine thereafter.

## Evidence

**Commands**, all four in one invocation on the final tree, `--rerun-tasks`:

```
./gradlew :app:assembleDebug :app:testDebugUnitTest :app:validateDebugScreenshotTest :app:lintDebug
BUILD SUCCESSFUL in 42s
```

| Check | Result | Before this task |
|---|---|---|
| `assembleDebug` | `BUILD SUCCESSFUL` | — |
| `testDebugUnitTest` | **113 tests, 0 failures** | 63 |
| `validateDebugScreenshotTest` | **16 cases, 0 failures** | 8 |
| `lintDebug` | **`0 errors, 64 warnings`** | `0 errors, 61 warnings` |

The unit count is read from `app/build/test-results/testDebugUnitTest/TEST-*.xml`, not the
console: 12 `DayCellStateTest` + 11 `CalendarMonthTest` + 27 `CalendarViewModelTest` are new here.
A count that had **not** gone up would have meant the task did not run (see `knowledge/PROJECT.md`
§ *One failing Gradle task aborts the others*).

### Acceptance, criterion by criterion

- **Build, unit tests and lint succeed** — above.
- **A JVM test proves the day-cell mapping** — `DayCellStateTest`, 12 cases: after-today →
  `Future`, today → `Today`, before-today → `Past`, a neighbouring month and `null` →
  `OutsideMonth`. Month boundaries (the 1st and the 30th of the displayed month, and today
  *being* the last day of its month) and leap-year 29 February are all covered.
- **A test proves the grid's shape** — `CalendarMonthTest`, 11 cases: two leading blanks for a
  Tuesday start, none for a Sunday start, six for a Saturday start, 29 days in a leap February,
  the full day sequence with nothing missing or duplicated, and no spare empty week for a month
  that fits exactly.
- **No tap handler is reachable for a `Future` cell** — one `clickable` in the whole screen
  (`CalendarDayCell`), carrying `enabled = state.isSelectable`, and `Future.isSelectable` is
  false. No `pointerInput`, no `toggleable`, no second handler. The ViewModel refuses a future
  date independently, for callers that never touch the grid.
- **Screenshot evidence** — eight cases in `CalendarScreenshotTest.kt`: today+notes,
  today+picked+notes, picked+notes, plain+notes, future, future+notes, the full month grid, and
  the German bottom-bar tab. `:app:validateDebugScreenshotTest` passes. **The human approves
  these images once** — see *Awaiting human eyes* below.

### The tests were made to fail — seven mutations

`PLAN.md` § Verification: a green suite against correct code is indistinguishable from a green
suite against a tautology. Each mutation was marked with the literal word `MUTATION`, applied
alone or with disjoint others, and reverted; `grep -r MUTATION app/src` is clean.

| # | Mutation | Failed exactly |
|---|---|---|
| M1 | `DayCellState.of`: `isAfter(today)` → `isAfter(today.plusDays(1))` | the two boundary tests |
| M2 | `buildGrid`: leading blanks `+ 1` | 5 `CalendarMonthTest` cases |
| M3 | `buildGrid`: outer `% 7` dropped from trailing blanks | the spare-week and last-square tests |
| M4 | `selectDate`: refusal guard disabled | the two future-selection tests |
| M5 | `refreshToday`: the new selection clamp removed | the backwards-clock drop test |
| M6 | `millisUntilNextDay`: `plusDays(1)` dropped | both wait tests |
| M7 | `CalendarMonthGrid`: `isSelected = false` | **`MonthGrid` screenshot** |

Two of these are worth carrying forward. **M7 proves the prior run's defect 2 is now
machine-defended**: dropping the selection on its way into the grid fails the full-grid image and
**passes every day-cell image**, because those call `CalendarDayCell` directly with
`isSelected = true` and so cannot see a grid-level wiring bug. Only the grid-level case can.
And **three mutations correctly failed nothing extra** — M1 left the October sweep green (it
shifts the boundary by a day, not the month), M3 left "always a whole number of weeks" green (a
spare week is still a multiple of seven), M6 left the never-zero test green (the floor still
holds). Tests that stay green under an unrelated mutation are tests that mean what they say.

### Fresh-context review

16 findings: **1 Critical, 2 Major, 13 Minor**. Twelve fixed here, three filed, one recorded as a
product call.

- **Critical — DoD criterion 9 was knowingly false for an unbounded window.** `refreshToday()`
  ran only from `onResume`, so a rollover while the screen sat in front of the user left the grid
  drawing the real today as `Future`: dimmed and refusing taps. The `ISSUES.md` entry filed for
  it argued the fix needed "new machinery… a broadcast receiver", and then four lines later gave
  the resolution as a `LaunchedEffect` — **the entry refuted its own blocker**. Fixed:
  `millisUntilNextDay()` on the ViewModel (testable against the injected clock) and a keyed
  `LaunchedEffect` loop in the Fragment. Three new tests; entry deleted.
- **Major — a backwards clock could strand the selection in the future.** Every argument in the
  code assumed the clock only moves forward. Flying west, a manual correction or an NTP step-back
  all make `LocalDate.now()` earlier, leaving the picked day after the new today — drawn with a
  full-strength selection ring on a square that is dimmed and refuses taps. `CalendarUiState`'s
  KDoc asserted this was impossible ("Never a day after `today`"); the guard it named only ran on
  the tap path. Fixed with a clamp in `refreshToday`, two tests (one for each direction), and a
  KDoc that now says *enforced*, not *impossible*.
- **Major — the bottom-bar label was shrunk 14sp → 11sp** to fit a third tab, charging every
  screen and every language a legibility cost to fit one English word, and still not fitting
  German. Reverted to 14sp. `basicMarquee` — the house rule's answer — was considered and
  rejected for the same reason `HomeNoteList` already rejects it. The truncation is now a
  **recorded defect with a picture**: `BottomBarElement` was made `internal` and a `locale = "de"`
  case at the real 66dp slot shows "Einstell…".
- **Minor, fixed:** three test assertions that could not fail, now labelled as shape checks
  rather than left to pad the count; a test whose name promised two properties and checked one,
  now checking both; the contrast KDoc citing the 3:1 non-text floor for a 14sp number (the real
  argument is that disabled controls are exempt from 4.5:1 — 3.95:1 is chosen to be legible, not
  to pass a threshold); a missing `Future`+`hasNotes` reference image, the one combination the
  component's own KDoc argues is reachable; `onClickLabel` carried on disabled squares; the
  `DateTimeFormatter` rebuilt on every note-store emission; and the new tab icon at
  `strokeWidth 1.5` where both siblings use `1`.
- **Minor, filed:** day-cell touch targets at ~43.4dp against Android's 48dp minimum — accepted
  with the arithmetic showing seven 48dp columns cannot fit a 360dp screen (`knowledge/ISSUES.md`).
- **The review verified every contrast number in the diff** against `ui/theme/Color.kt` and found
  them honest: 2.14:1 at 38%, 3.95:1 at 60%, 2.18:1 for `outlineVariant`. Only the *threshold*
  they were argued against was wrong.

### Recovered, not written, and it took three invocations

This iteration began at Recover. The tree held T-006 essentially complete, stamped 16:41–17:11 on
09-10 and 11:06–11:08 on 09-11 against a 16:30 checkpoint with `.ai/` untouched — so **two**
previous invocations had died mid-task. `grep -r MUTATION app/src` came back clean (iteration 7's
lesson, applied first), and all four commands were green on the debris before anything was built
on it (ENGINE.md §6.1). What was missing was not code: the debris's own `ISSUES.md` entries cited
"the fresh-context review of T-006, finding 1 / finding 3", so a review had been run and its
output died with the process. **A review whose findings cannot be read is not evidence**, so it
was re-run from scratch — and it returned a Critical the first one had filed rather than fixed.

## Awaiting human eyes

Eight reference images are recorded and none has been approved by a person. The engine has looked
at all of them and believes they are right, which `STATE.md` already records is **not** approval.

1. **The full month grid** — that the weekday labels sit over the right columns (1 September 2026
   is a Tuesday, so the first row starts with two blanks), and that the past/future boundary reads
   as a change of weight partway through the second week rather than as damage.
2. **Today with notes** — that the dot survives on the filled square (6.75:1, the defect a prior
   run shipped here).
3. **Today, picked, and with notes** — the worst overlap in the screen and the state the app opens
   on: is a day that is *both* today and picked distinguishable from a day that is only today?
4. **Picked with notes**, and **plain with notes** — that the selection ring reads as a selection.
5. **A day still to come** — **DoD criterion 9's perceptual clause.** Is it *legibly* disabled:
   quieter than a live day without looking like a rendering fault? Only a person can say.
6. **A day still to come, with notes** — the dimmed dot at 3.16:1. Dot or smudge?
7. **The German bottom-bar tab** — this one is a **known defect, not a proposal**. "Einstell…" is
   what a German user sees. Approving the image means agreeing it is the right thing to have a
   picture of, not that the truncation is acceptable.

Plus one product call the machine made and a human may want back: **the grid starts weeks on
Sunday** (`CalendarMonth.WEEK_START`), as this task's notes directed, matching the prior run.
`values-de/` is a shipped locale and the German convention is Monday-first, so the grid will read
as foreign to a German user. Changing it is one line and a re-record of every grid image.

## Notes

### Three regressions a prior run already shipped here — read before writing the grid

`loop/todo-calendar-screens` built this same screen once. Its screenshot suite names exactly what
went wrong, and **the unit suite was green for all three**. Read the file first:
`git show loop/todo-calendar-screens:app/src/screenshotTest/kotlin/com/example/skeleton/screenshot/CalendarScreenshotTest.kt`

1. **A has-notes dot drawn in `primary` on top of a `primary`-filled today cell — invisible**, on
   exactly the day a user looks at first. A test can assert the date is in `datesWithNotes`; it
   cannot see that the marker vanished.
2. **Selection never reached the grid** — `selectedDate` was computed and never passed down.
3. **A selected day with no notes rendered an unexplained blank gap** (that one lands in T-007).

Write the screenshot cases with the grid, using the harness imported in T-010 and the prior file as
the pattern — at minimum: today+hasNotes, today+selected, selected+hasNotes, plain+hasNotes, and the
full month grid. These are the combinations, not the individual states, and the combinations are
where it broke.

### Other

- `java.time` is available on `minSdk 24` through core-library desugaring (already enabled). Use
  `LocalDate`, `YearMonth` and `DayOfWeek` rather than `Calendar`.
- The week start day is locale-dependent. Pick one deliberately and say which in the KDoc — a grid
  that assumes Monday on a locale that starts on Sunday is off by one for every cell. **The prior run
  chose Sunday-first**, and its screenshot suite pins the weekday header against the grid for that
  reason. Match it unless there is a reason not to, and say which you chose.
- Do not import a third-party calendar library. That would be a technology choice, i.e. Tier 2, and
  needs an escalation first.
- **Draw day-cell boundaries from `outline`, not `outlineVariant`.** T-001 set them up as a
  deliberate pair: `outline` (`#55616E`) clears 3:1 against the black ground and is for edges that
  carry meaning; `outlineVariant` (`#232B34`) is intentionally near-invisible and is for decorative
  dividers only. `outline` itself was caught at 2.19:1 during T-001 and raised — on pure black a
  too-dark border does not read as subtle, it reads as absent, and a grid with no visible cell edges
  is the kind of thing every automated check passes.
