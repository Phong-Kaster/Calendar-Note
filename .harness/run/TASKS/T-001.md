# T-001 - The app is always dark

- **Status:** complete (iteration 2)
- **Depends on:** -
- **Blocked by decision:** - (D-001, D-002 answered)
- **Attempts:** 2 / 3 (attempt 1 Fast failed lint; attempt 2 Capable passed)
- **Declared File Scope:** `app/src/main/java/com/example/skeleton/ui/theme/Theme.kt`,
  `app/src/main/java/com/example/skeleton/ui/theme/Color.kt`,
  `app/src/main/java/com/example/skeleton/core/CoreLayout.kt`,
  `app/src/main/java/com/example/skeleton/core/CoreActivity.kt`,
  `app/src/main/res/values/themes.xml`
- **Model Tier:** Fast

## Description

`CoreLayout` paints every screen black while `MyApplicationTheme` follows the phone's light/dark mode and
dynamic colour, so in light mode any colour taken from the theme is dark-on-black. Make the app use one fixed
dark colour scheme and a matching window/system-bar style.

## Acceptance

- `MyApplicationTheme` always uses one `darkColorScheme(...)` that sets **every** role it defines (primary,
  onPrimary, primaryContainer, onPrimaryContainer, secondary…, tertiary…, background, onBackground, surface,
  onSurface, surfaceVariant, onSurfaceVariant, outline, outlineVariant, error, onError, …); no
  `isSystemInDarkTheme()` and no dynamic colour branch. Colour values live in `Color.kt`.
- `CoreLayout.kt` takes its background from `MaterialTheme.colorScheme.background` (no `Color.Black`).
- `CoreActivity.kt` calls `enableEdgeToEdge(statusBarStyle = SystemBarStyle.dark(...), navigationBarStyle = SystemBarStyle.dark(...))`.
- `res/values/themes.xml` sets `android:windowLightStatusBar` to `false` and a dark window background.
- Observable: in the phone's light mode the status-bar icons are light and screens stay dark.

## Evidence

- Iteration 2: `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` → `BUILD SUCCESSFUL`,
  lint 0 errors. Review: dark scheme defines every role; bars dark in light mode. Unfixed non-blocking
  review N-4: window background `@android:color/black` vs theme `#0B0D10` (slight shade change on cold start).

## Failed attempts

### Attempt 1 (iteration 2, Fast tier) — failed lint
- Command: `./gradlew :app:lintDebug`
- Error tail:
  ```
  app\src\main\res\values\themes.xml:14: Error: android:windowLightNavigationBar requires API level 27 (current min is 24) [NewApi]
          <item name="android:windowLightNavigationBar">false</item>
  Lint found 1 error, 58 warnings. BUILD FAILED in 26s
  ```
- Scope reverted to checkpoint; task escalated to Capable tier (mechanical rule). Constraint C-10 recorded.

## Notes
