# T-006 - Calendar: add a note to a date, select a date to see only its notes, dates with notes marked

- **Status:** complete
- **Depends on:** T-005
- **Blocked by decision:** -
- **Attempts:** 1 / 3
- **Declared File Scope:**
  - `app/src/main/java/com/example/skeleton/domain/model/Note.kt`
  - `app/src/main/java/com/example/skeleton/domain/repository/NoteRepository.kt`
  - `app/src/main/java/com/example/skeleton/data/database/local/entity/NoteEntity.kt`
  - `app/src/main/java/com/example/skeleton/data/database/local/dao/NoteDao.kt`
  - `app/src/main/java/com/example/skeleton/data/mapper/NoteMapper.kt`
  - `app/src/main/java/com/example/skeleton/data/repository/impl/NoteRepositoryImpl.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/CalendarUiState.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/CalendarViewModel.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/CalendarFragment.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/component/CalendarDayCell.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/component/CalendarNoteList.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/component/CalendarAddNoteRow.kt`
  - `app/src/test/java/com/example/skeleton/calendar/FakeNoteRepository.kt`
  - `app/src/test/java/com/example/skeleton/calendar/NoteDateScopingTest.kt`
- **Model Tier:** Capable <!-- introduces the NoteRepository/NoteEntity/NoteDao contract for the first
     time, plus the date-storage-format decision (epochDay) and the "visually distinguishable" judgment
     call on the marker -->

## Description

Second vertical slice, and the largest task in the run — kept as one checkpoint (rather than splitting the
"add note" and "mark dates with notes" behaviors) because they are two views of the same underlying
date-scoped query and splitting them would leave one half with no independent observable behavior.

- `Note` domain model: `id: Long`, `epochDay: Long` (the date, stored as `LocalDate.toEpochDay()` — no
  existing `LocalDate` `TypeConverter` in this repo, only `DateConverter` for `java.util.Date`; storing a
  plain `Long` avoids needing a new converter and keeps date-range queries simple integer comparisons),
  `title: String`, `createdAt: Long`.
- `NoteEntity` (`@Entity(tableName = "notes")`, indexed on `epochDay`) + `NoteDao`:
  `observeByEpochDay(epochDay: Long): Flow<List<NoteEntity>>`,
  `observeEpochDaysWithNotesBetween(start: Long, end: Long): Flow<List<Long>>` (drives the "has notes"
  marker for the visible month), `suspend fun insert(entity: NoteEntity)`.
- `NoteMapper.kt`, `NoteRepository` interface + Room-backed `NoteRepositoryImpl`.
- `CalendarUiState` gains: `selectedDate: LocalDate?`, `notesOfSelectedDate: List<Note>`,
  `datesWithNotes: Set<LocalDate>` (recomputed whenever the visible month changes).
- `CalendarViewModel` gains `noteRepository: NoteRepository` (constructor param — the Iteration updates
  `ViewModelModule.kt`'s binding after this Phase), `selectDate(date)`, `addNote(title)`.
- `CalendarDayCell` renders a marker (e.g. a dot) when its date is in `datesWithNotes`; tapping a day
  calls `selectDate`. `CalendarNoteList` shows `notesOfSelectedDate` only. `CalendarAddNoteRow` is the
  title input + add button, enabled only when a date is selected.
- `FakeNoteRepository` (in-memory, `MutableStateFlow<List<Note>>`-backed) + `NoteDateScopingTest`: a note
  added to one date is returned for that date and not for another; a month's `datesWithNotes` contains
  exactly the dates with notes, excluding dates in adjacent months; an empty date returns an empty list.

Do **not** touch `AppDatabase.kt`, `Migration.kt`, `injection/*`, or `strings.xml` — the Iteration wires
those itself.

## Acceptance

- Tapping a date, typing a note title, and adding it shows the note under that date only; other dates'
  note lists are unaffected.
- A date with at least one note renders differently (marker) from one without, and the marker updates as
  notes are added.
- `NoteDateScopingTest` passes under `gradlew.bat test`.
- Maps to DoD criteria 6, 19, 20, 21, 24, 25.

## Evidence

Dispatched at Capable tier — succeeded on attempt 1. `Note`/`NoteEntity`/`NoteDao`/`NoteMapper`/
`NoteRepository`/`NoteRepositoryImpl` built as designed (dates cross the domain boundary as `LocalDate`;
only `NoteRepositoryImpl` translates to/from the `epochDay` `Long` Room stores). `CalendarViewModel` gained
`noteRepository`, `selectDate(date)`, `addNote(title)`, and two `flatMapLatest` collectors (notes for the
selected date; dates-with-notes for the visible month, both re-keyed via `distinctUntilChanged()`).
`CalendarDayCell` renders a small dot marker when `hasNotes` is true; `CalendarNoteList`/`CalendarAddNoteRow`
are new components. `FakeNoteRepository` + `NoteDateScopingTest` (3 tests: date scoping, month-range
exclusion of adjacent-month dates, empty-date empty list) all pass. `gradlew.bat assembleDebug` and
`gradlew.bat test` both pass on the combined Phase 2 tree (14 tests, 0 failures). `gradlew.bat lintDebug`
shows only the 4 pre-existing baseline errors (unrelated); the two new hardcoded-English strings the
Worker flagged (`CalendarAddNoteRow`'s hint/button text) were moved to `stringResource` by the Iteration
using new `note_title`/`delete_task` keys with German translations, adding no new lint errors.

The Worker correctly identified and reported (not fixed, since out of its Declared File Scope) that
`CalendarMonthGrid.kt` hardcoded `hasNotes = false` for every cell and had no parameter to thread
`datesWithNotes` through. The Iteration added a `datesWithNotes: Set<LocalDate> = emptySet()` parameter to
`CalendarMonthGrid` and wired `CalendarFragment` to pass `uiState.datesWithNotes` — this is why the
Phase's shared-file list now includes `CalendarMonthGrid.kt` (not anticipated in `PLAN.md`'s original Phase
2 row; logged in `AMENDMENTS.md` Iteration 2). Fresh-Context Review (clean context) found no defects in
this task's own code; its one "major" finding (no edit/delete-note operation exists yet) is exactly T-007,
correctly out of this Phase's scope.

## Failed attempts

## Notes
