# T-004 - To-do: edit an existing task's title

- **Status:** pending
- **Depends on:** T-003
- **Blocked by decision:** -
- **Attempts:** 0 / 3
- **Declared File Scope:**
  - `app/src/main/java/com/example/skeleton/domain/repository/TaskRepository.kt`
  - `app/src/main/java/com/example/skeleton/data/database/local/dao/TaskDao.kt`
  - `app/src/main/java/com/example/skeleton/data/repository/impl/TaskRepositoryImpl.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/todo/TodoUiState.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/todo/TodoViewModel.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/todo/component/TodoEditTaskDialog.kt`
  - `app/src/test/java/com/example/skeleton/todo/TaskEditTitleTest.kt`
- **Model Tier:** Capable <!-- introduces a new dialog + edit-mode UiState fields: an open-ended state
     shape, not a mechanical extension of an existing pattern -->

## Description

Add `suspend fun updateTitle(id: Long, title: String)` to `TaskRepository` / `TaskDao` /
`TaskRepositoryImpl` (reject blank/whitespace titles, same rule as add). Add an edit entry point on each
`TodoTaskItem` (e.g. tap-to-edit or a pencil icon) opening `TodoEditTaskDialog`, with `TodoUiState` tracking
which task (if any) is being edited. This completes full CRUD for tasks.

## Acceptance

- Editing a task's title updates it in the list.
- `TaskEditTitleTest` (fake repository): rename persists and is visible via `tasksFlow`; a blank rename is
  rejected (title unchanged); renaming an unknown id is a no-op.
- Maps to DoD criteria 5, 15 — and closes out the full task-CRUD requirement (DoD 3–5, 12–16 together).

## Evidence

## Failed attempts

## Notes
