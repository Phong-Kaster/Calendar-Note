# T-004 - Notification opens the app and swipe-away stops a paused player

- **Status:** pending
- **Depends on:** T-003
- **Blocked by decision:** D-001 (DoD approval)
- **Attempts:** 0 / 3
- **Declared File Scope:** `app/src/main/java/com/example/skeleton/data/service/MusicPlaybackService.kt`
- **Model Tier:** Capable

## Description

Finish the notification/foreground behaviour (A-003): `setSessionActivity` with an immutable `PendingIntent` to
`MainActivity` using flags that reuse the existing task (no second copy); `onTaskRemoved` stops the service when the
player is not playing (or has no media), keeps it when playing; release player and session in `onDestroy`.

## Acceptance

- DoD 12 and 13 on device; DoD 1 green.

## Evidence

## Failed attempts

## Notes

- Critique at bootstrap: POST_NOTIFICATIONS is not requested at runtime unless DoD 11's "permission denied" check fails.
