# T-004 - Notification permission, tap notification opens Music, swipe-away stops a paused player

- **Status:** deferred
- **Depends on:** T-003
- **Blocked by decision:** D-002 (D-001 answered)
- **Attempts:** 0 / 3
- **Declared File Scope** (paths under `app/src/main/java/com/example/skeleton/` unless stated):
  - modified `service/MusicPlaybackService.kt`
  - new `service/PlaybackStopPolicy.kt` (pure `fun shouldStopOnTaskRemoved(isPlaying: Boolean, mediaItemCount: Int): Boolean`)
  - modified `MainActivity.kt` (open the Music tab when started from the notification: `onCreate` + `onNewIntent`)
  - new `ui/fragment/music/component/MusicNotificationPermissionRequest.kt`
  - modified `ui/fragment/music/MusicFragment.kt`
  - new `app/src/test/java/com/example/skeleton/service/PlaybackStopPolicyTest.kt`
- **Model Tier:** Capable

## Description

- On Android 13+, the first tap on a song asks for `POST_NOTIFICATIONS`; playback starts whether it is granted
  or refused (asked once per screen visit at most).
- The session's activity `PendingIntent` opens `MainActivity` with an extra that makes it navigate to the
  Music tab (`toMusic`), reusing the existing task.
- `onTaskRemoved`: if `PlaybackStopPolicy.shouldStopOnTaskRemoved(...)` → `pauseAllPlayersAndStopSelf()` (C-09);
  release the session and player in `onDestroy`.

## Acceptance

- `PlaybackStopPolicyTest`: playing with items → keep; paused with items → stop; empty queue → stop.
- `onTaskRemoved` calls `PlaybackStopPolicy` (DoD #5, stop part).
- DoD #13, #15, #16.

## Evidence

## Failed attempts

## Notes

- Iteration wiring: strings; `MainActivity` `launchMode` in the manifest if needed; README.
