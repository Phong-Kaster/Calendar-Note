# T-004 - Now Playing shows the real album art

- **Status:** blocked
- **Depends on:** T-001, T-003
- **Blocked by decision:** D-001
- **Attempts:** 0 / 3
- **Declared File Scope:** `app/src/main/java/com/example/skeleton/ui/fragment/nowplaying/component/NowPlayingArtwork.kt`
- **Model Tier:** Capable

## Description

Fill `NowPlayingArtwork(albumArtUri: String?)`: load the picture with `LocalContext.current.contentResolver`
(`openInputStream(Uri.parse(...))` + `BitmapFactory.decodeStream`, downsampled to about the display size) on
`Dispatchers.IO` via `produceState(key = albumArtUri)`; show it cropped square with rounded corners; any failure or
`null` → the existing placeholder (never a crash, never a blank area). The art changes when the song changes.

## Acceptance

- Build + lint green; no new dependency.
- Human DoD #7 (art or visible placeholder) and #9 (art updates on song change).

## Evidence

## Failed attempts

## Notes
