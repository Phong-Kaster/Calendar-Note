# T-004 - Notification opens the app and swipe-away stops a paused player

- **Status:** complete (iteration 3)
- **Depends on:** T-003
- **Blocked by decision:** —
- **Attempts:** 2 / 3 (attempt 1 built green but failed review B-1; attempt 2 accepted)
- **Declared File Scope:** `app/src/main/java/com/example/skeleton/data/service/MusicPlaybackService.kt`, `app/src/main/java/com/example/skeleton/MainActivity.kt` (added iteration 3, Tier-1)
- **Model Tier:** Capable

## Description

Finish the notification/foreground behaviour (A-003): `setSessionActivity` with an immutable `PendingIntent` to
`MainActivity` using flags that reuse the existing task (no second copy); `onTaskRemoved` stops the service when the
player is not playing (or has no media), keeps it when playing; release player and session in `onDestroy`.

## Acceptance

- DoD 12 and 13 on device; DoD 1 green.

## Evidence

- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` exit 0 (iteration 3): 34 tests in 4 suites, 0 failures; lint 0 errors, 63 warnings (unchanged).
- Session activity: immutable `PendingIntent` → `MainActivity` with `EXTRA_OPEN_MUSIC`, `NEW_TASK|CLEAR_TOP|SINGLE_TOP`; `MainActivity` handles it in `onCreate` (fresh start) and `onNewIntent` via global `toMusic` (pops to graph root), skipped when Music is already current.
- `onTaskRemoved`: keeps running only when `playWhenReady && mediaItemCount > 0 && state ∉ {IDLE, ENDED}`; otherwise `pauseAllPlayersAndStopSelf()` (Media3 1.8.0 API, resolved at compile time).
- Device: not exercised — the device disconnected mid-install (`adb: no devices/emulators found`). DoD 12/13 are `human` either way.

## Failed attempts

- **Attempt 1 (iteration 3, Capable):** build/test/lint green, rejected by fresh review. B-1: the PendingIntent reused the task (MAIN/LAUNCHER + `NEW_TASK|RESET_TASK_IF_NEEDED`) but never navigated, so with the app left on Home/Setting the notification reopened Home and needed two Backs to leave (DoD 12). Also N-1: bare `stopSelf()` does not end a service still bound by the app's `MediaController`; N-2: a failed (IDLE, `playWhenReady=true`) player counted as playing. Command: `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` (exit 0 — failure was review, not build).

## Notes

- Critique at bootstrap: POST_NOTIFICATIONS is not requested at runtime unless DoD 11's "permission denied" check fails.
