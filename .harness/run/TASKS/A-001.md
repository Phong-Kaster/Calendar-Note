# A-001 — Alarms is the fourth bottom-bar tab, and the screen opens and says it is empty

- **Status:** deferred
- **Depends on:** –
- **Blocked by decision:** D-001, D-002, D-003 (D-003 blocks completion only — see its entry)
- **Attempts:** 0 / 3
- **Declared File Scope:**
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsFragment.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsUiState.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsViewModel.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarms/component/AlarmsEmptyState.kt`
  - CREATE `app/src/main/res/drawable/ic_bottom_alarm.xml`
- **Model Tier:** Capable — (a) fails: the change is only meaningful alongside `BottomBarDestination.kt`,
  `navigation_graph.xml`, `ViewModelModule.kt` and both `strings.xml`, all Iteration-owned. (c) fails:
  what the bottom bar's centre "+" means on a fourth screen is a cross-screen contract that
  `CoreBottomBar`'s KDoc deliberately refuses to give a default for.

## Description

Put an Alarms screen in the app and make it reachable. Nothing is stored yet: the ViewModel holds an empty
list and the screen shows its empty state. The ViewModel exists because the house screen spine requires one,
not because it does anything yet.

Build the standard spine — `AlarmsFragment : CoreFragment()` with `by viewModel()`, a private
`AlarmsLayout(uiState, on…: () -> Unit = {})` that navigates nowhere, `AlarmsUiState` as a data class with
every field defaulted, `AlarmsViewModel` with an instance `private val TAG` and
`_uiState.value = _uiState.value.copy(...)` updates. Copy `CalendarFragment` for the shape.

Use `CoreLayout`, with `topBar = { CoreTopBar(title = …) }` and `bottomBar = { CoreBottomBar(...) }`. Leave
`CoreLayout`'s `floatingActionButton` slot empty for now — A-002 fills it, because a FAB that opens nothing
is not a checkpoint.

The empty state is a centred sentence in the content area, not a blank region. `HomeNoteList`'s empty state
is the thing to match, so the two read as the same deliberate choice.

The `ic_bottom_alarm.xml` drawable: copy `ic_bottom_calendar.xml`'s shape — a 25dp stroke vector whose
`strokeColor` is overridden by `Icon(tint = …)` at the call site.

**The Iteration handles, and you must not touch:** `domain/enums/BottomBarDestination.kt` (the fourth entry),
`res/navigation/navigation_graph.xml` (`alarmsFragment` + a `toAlarms` action shaped like `toCalendar`),
`injection/ViewModelModule.kt`, both `strings.xml` files, `ui/component/CoreBottomBar.kt` (hiding the
centre "+" on this screen, per AS-5), `README.md`, and the re-record of the bottom-bar reference images.

**Report, do not write:** every new string key with its English text and a German translation.

## Acceptance

The user can tap a fourth tab in the bottom bar and land on an Alarms screen that tells them there are no
alarms yet.

- DoD 1, 2 — build, tests, lint, screenshots green
- DoD 8 — the tab can be found and it is obvious which screen you are on *(human)*
- DoD 10 — the empty screen says it is empty rather than looking broken *(human)*
- DoD 33, 34 — no hardcoded colour, no hardcoded string
- DoD 36 — README package tree updated *(Iteration)*
- A new `@PreviewTest` case pinning the empty state, sized per C-05

## Evidence

<!-- Filled on completion. -->

## Failed attempts

<!-- Written at the moment of failure. -->

## Notes

- The centre "+" question is AS-5 / D-001 option A. If the human picks a different option, this task's
  `CoreBottomBar` integration changes but its own five files do not.
- Adding the fourth entry is what breaks `ThemeScreenshotTestKt/BottomBar*` — see PLAN.md risk 1. That
  happens in a file this task never opens.
- Keep the tab's label short in both English and German: the three-tab bar already truncates
  "Einstellungen", and a fourth slot is narrower still.
