# A-007 — The knowledge documents describe the run that happened

- **Status:** complete (iteration 8)
- **Depends on:** A-001, A-002, A-003, A-004, A-005, A-006
- **Blocked by decision:** – (D-001 decided 2026-09-13; see AMENDMENTS.md)
- **Attempts:** 1 / 3
- **Declared File Scope:**
  - MODIFY `.harness/knowledge/PROJECT.md`
- **Model Tier:** Capable — (b) fails: deciding what was actually learned, and which of it is a **trap**
  rather than a preference, is pure judgement. A wrong call here is invisible now and expensive later,
  because the next run reads this file and believes it.

## Description

`PROJECT.md` currently describes a repository with **no** notification and alarm infrastructure — it says
so in as many words, and it will be wrong the moment A-004 lands. This task makes it true again.

What must change, at minimum:

- **Environmental Facts** — "Notification and alarm infrastructure is 100% absent" is no longer true.
  Replace it with what now exists: the channel and where it is created, the receivers and what they hear,
  the scheduler seam and why it is a seam.
- **Architecture Conventions** — `AppDatabase` is no longer at `version = 3`. The bottom bar is no longer
  three tabs. `CoreLayout`'s `floatingActionButton` slot is no longer unused by anything.
- **Toolchain** — the unit-test count, the lint summary and the screenshot case count all moved. Quote
  numbers only from a run whose own `BUILD SUCCESSFUL` line you were shown; a count that did not move after
  tests were added is proof the task did not run, not proof of a pass.
- **Constraints** — add a `C-xx` entry for anything this run discovered that would make a future Worker
  **wrong** rather than merely untidy. Each entry carries its evidence: the iteration, the command, what
  actually happened. Do not add a Constraint for something that was only annoying; the list works because
  every entry on it is a trap, and padding it is how it stops being read.

Each claim is checked against the tree as it now stands, not against what the task files said would be
built. Where they differ, the tree is right and the task file was a plan.

**The Iteration handles, and you must not touch:** `README.md` (package tree and feature table — DoD 36),
and `.harness/ISSUES.md`, which the Iteration regenerates every iteration by §10 and which therefore cannot
be in any Worker's scope. See AMENDMENTS.md A-1.

## Acceptance

A person reading `.harness/knowledge/PROJECT.md` with no memory of this run gets a true description of the
repository, and a person starting the next run is warned about every trap this one found.

- DoD 36 — README's package tree and feature table describe the app that now exists *(Iteration)*
- No claim in `PROJECT.md` contradicts the tree at the checkpoint
- Every new Constraint carries its evidence

## Evidence

- `.harness/knowledge/PROJECT.md` brought current: Environmental Facts' "100% absent" claim replaced with
  a sourced inventory (channel, both receivers, the scheduler seam, the permission notice, the manifest
  additions); Architecture Conventions updated for the four-tab bottom bar, the FAB's one user, the
  `alarms` table columns, `schedulerModule`, and the per-store clock pattern; the Toolchain table's stale
  "iteration 11" figures replaced with 251 tests / 15 classes / 0 failures, lint 0 errors / 72 warnings,
  23/23 screenshot cases, all cross-checked against the tree independently rather than copied from a task
  file; the stale "capability ledger not installed" paragraph corrected.
- Two new Constraints added, each with evidence: **C-16** (an Activity's launch `Intent` replays on every
  recreation — `savedInstanceState == null` guard needed, from `AMENDMENTS.md` A-12 #1) and **C-17**
  (revoking the exact-alarm permission cancels pending alarms; granting it back arms nothing without an
  explicit re-arm on the transition — from `AMENDMENTS.md` A-14 #1).
- A Fresh-Context Review of the diff found one MAJOR (C-06's addition overstated the seam as "the only
  part of the alarms feature any test can see", contradicting the 85 tests that exist over
  `AlarmRepositoryImpl`/`AlarmsViewModel`/`AlarmEditorViewModel` and contradicting C-17's own citation nine
  lines later) and one MINOR (`AlarmNotifier` exposes five constants, not four — mirrored from a KDoc
  comment in the source file itself). Both fixed by the Iteration this checkpoint: C-06's claim corrected
  to state the seam is what makes the *scheduling decision* testable, not the only tested alarms code;
  `AlarmNotifier.kt`'s KDoc corrected from "four constants" to "five".
- DoD 36 (README's package tree and feature table describe the app that now exists) was already satisfied
  going into this task — `README.md` was kept current in the same change as each prior task (A-13's
  wiring); re-checked this checkpoint and still accurate, no edit needed.
- No claim in the diff contradicts the tree at the checkpoint (reviewer spot-checked 5+ claims against
  source: `AlarmNotifier.kt`'s channel setup, `MainActivity.kt`'s intent guard, `BottomBarDestination.kt`'s
  four entries, `SchedulerModule.kt`'s Koin wiring, `AppDatabase.kt`'s version and migration registration
  — all confirmed correct).

## Failed attempts

<!-- None — the Worker's diff needed two review-driven corrections, made directly by the Iteration; no
     attempt was reverted or re-dispatched. -->

## Notes

- The **Provenance** note at the top of `PROJECT.md` explains the `knowledge/` → `.harness/knowledge/`
  move. The root `knowledge/` directory is prior-harness residue and stays untouched — the engine cannot
  delete files (C-12) and must not try.
- The German bottom-bar truncation (PLAN.md risk 6) predates this feature. It belongs in `ISSUES.md` as a
  standing defect, not in `PROJECT.md` as a constraint, and it is the Iteration that writes it there.
