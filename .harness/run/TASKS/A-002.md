# A-002 — An alarm can be written, saved and listed, and it is still there after a restart

- **Status:** pending
- **Depends on:** A-001
- **Blocked by decision:** – (D-001, D-002 decided 2026-09-13; see AMENDMENTS.md)
- **Attempts:** 0 / 3
- **Declared File Scope:**
  - CREATE `app/src/main/java/com/example/skeleton/domain/model/Alarm.kt`
  - CREATE `app/src/main/java/com/example/skeleton/domain/model/BlankAlarmMessageException.kt`
  - CREATE `app/src/main/java/com/example/skeleton/domain/repository/AlarmRepository.kt`
  - CREATE `app/src/main/java/com/example/skeleton/data/database/local/entity/AlarmEntity.kt`
  - CREATE `app/src/main/java/com/example/skeleton/data/database/local/dao/AlarmDao.kt`
  - CREATE `app/src/main/java/com/example/skeleton/data/mapper/AlarmMapper.kt`
  - CREATE `app/src/main/java/com/example/skeleton/data/repository/impl/AlarmRepositoryImpl.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarm_editor/AlarmEditorFragment.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarm_editor/AlarmEditorUiState.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarm_editor/AlarmEditorViewModel.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarm_editor/component/AlarmEditor.kt`
  - CREATE `app/src/main/java/com/example/skeleton/ui/fragment/alarms/component/AlarmRow.kt`
  - CREATE `app/src/test/java/com/example/skeleton/data/repository/AlarmRepositoryImplTest.kt`
  - CREATE `app/src/test/java/com/example/skeleton/ui/fragment/alarm_editor/AlarmEditorViewModelTest.kt`
  - MODIFY `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsFragment.kt`
  - MODIFY `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsUiState.kt`
  - MODIFY `app/src/main/java/com/example/skeleton/ui/fragment/alarms/AlarmsViewModel.kt`
- **Model Tier:** Capable — (a) fails: `AppDatabase.kt`, `Migration.kt`, `DatabaseModule.kt`,
  `RepositoryModule.kt`, `ViewModelModule.kt` and `navigation_graph.xml` are all Iteration-owned.
  (c) fails: the entity schema, the time representation, and a hand-written migration are architecture
  decisions behind a one-way door.

## Description

The whole persistence vertical plus the editor screen, in one checkpoint because no part of it is
demonstrable alone: splitting at the persistence boundary leaves one half with no caller.

**Schema, decided now and final.** `AlarmEntity` carries: `id` (autogenerate `Long`), `message` (`String`),
`hourOfDay` (`Int` 0–23), `minute` (`Int` 0–59), `enabled` (`Boolean`, default `true`), `createdAt`
(`Long`). Store the time as two `Int`s rather than through a converter — the same reasoning that made
`NoteEntity` store an epoch day as a raw `Long`: it keeps comparisons integer and confines the conversion to
`AlarmMapper.kt`.

**Why `enabled` ships now even though nothing reads it until A-004:** a second hand-written migration is the
most dangerous operation available in this repository (C-03) and paying that risk twice for one column is a
bad trade. It is dead weight for two Phases, deliberately.

**Scheduler data requirements, sketched now so the schema really is final** (PLAN.md risk 4): the scheduler
derives its `PendingIntent` request code from `id.toInt()`. Row ids here are sequential from 1, so the
truncation is safe and no separate request-code column is needed. No `lastFiredAt` is needed either — the
next fire time is computed from `hourOfDay`/`minute` against the injected `Clock`, never from history.
**If A-004 finds either of these wrong, that is a Tier-1 amendment and a second migration, not a silent
schema change.**

`AlarmRepository` follows `NoteRepository`: a non-suspend `val alarmsFlow: Flow<List<Alarm>>` doing the
time-ascending sort **in the repository, not in SQL**; `suspend fun getAlarm(id): Outcome<Alarm?>` (three
answers, so `Outcome<T?>` — C-09); `suspend fun save(alarm): Outcome<Unit>`. Take
`clock: Clock = Clock.systemDefaultZone()` and `ioDispatcher` as constructor parameters. A blank message is
refused as `Outcome.Error(throwable = BlankAlarmMessageException(...))` — tagged by type, never by message
text.

The editor screen is a Fragment (`alarm_editor/`), reached by a FAB on the Alarms screen — fill
`CoreLayout`'s existing `floatingActionButton` slot with a Material `FloatingActionButton` using
`containerColor = colorScheme.primaryContainer` / `contentColor = colorScheme.onPrimaryContainer`, which is
the contrast-verified pair (9.12:1). Give the list bottom `contentPadding` of at least the FAB's height plus
twice its margin, as a named constant, so the last row is never covered.

**The message field needs `Modifier.imePadding()` before `verticalScroll` (C-04)** — this is a full-screen
text input and the window is never resized when the keyboard opens.

Tests: hand-written `FakeAlarmDao` (returning a **jumbled** list, so the repository's own sort is what is
under test) and a `BrokenAlarmDao` failing every call to reach the catch blocks. ViewModel tests via
`Dispatchers.setMain(UnconfinedTestDispatcher())`. Do **not** write a test whose subject is a `Bundle` (C-06).

**The Iteration handles, and you must not touch:** `AppDatabase.kt` (entity list, `version = 4`,
`alarmDao()`), `Migration.kt` (`MIGRATION_3_4`, whose SQL it copies out of the generated
`AppDatabase_Impl.kt` after building your entity), `DatabaseModule.kt`, `RepositoryModule.kt`,
`ViewModelModule.kt`, `navigation_graph.xml`, both `strings.xml`, `AlarmsScreenshotTest.kt`, `README.md`.

## Acceptance

The user can tap the FAB, write a message, set a time, save, and see the alarm in the list — and it is still
there after the app is killed and reopened.

- DoD 5, 6, 7 — round-trip, fail-soft, repository-side ordering
- DoD 12 — nav destinations and arguments with defaults
- DoD 13, 16 — the FAB is visible and opens the editor; the editor is usable with the keyboard up *(human)*
- DoD 14, 15, 17 — stores exactly what was typed; blank message refused by type; readable from a fresh
  repository instance
- DoD 3, 4 — `version = 4` + `MIGRATION_3_4` registered; upgrade over a v3 install does not crash *(human)*
- DoD 9, 11 — the list scrolls and the last row is not under the FAB; a long message has defined overflow
  *(human)*
- DoD 33, 34, 36

## Evidence

## Failed attempts

## Notes

- The migration is a two-step the Worker cannot perform: write the entity, let the Iteration build, copy the
  generated `createAllTables` statement verbatim, then build again.
- A screenshot case of a populated row must not photograph a locale-formatted time — pass a pre-formatted
  `String` in, or the reference is host-locked (see PROJECT.md § references are exact-pixel and host-locked).
