# T-003 - Tapping a song plays it in a foreground media service; now-playing bar controls it

- **Status:** complete (iteration 2)
- **Depends on:** T-002
- **Blocked by decision:** —
- **Attempts:** 2 / 3
- **Declared File Scope:** (under `app/src/main/java/com/example/skeleton/` unless shown otherwise)
  - `domain/model/PlaybackState.kt`
  - `domain/repository/PlayerRepository.kt`
  - `data/repository/impl/PlayerRepositoryImpl.kt` (Media3 `MediaController`)
  - `data/mapper/MediaItemMapper.kt`
  - `data/service/MusicPlaybackService.kt` (`MediaSessionService` + ExoPlayer + `ForwardingPlayer`)
  - `ui/fragment/music/component/NowPlayingBar.kt`
  - `ui/fragment/music/MusicFragment.kt`, `MusicUiState.kt`, `MusicViewModel.kt`
  - `app/src/test/java/com/example/skeleton/ui/fragment/music/MusicViewModelTest.kt`
- **Model Tier:** Capable

## Description

Playback through Media3 (A-001). `MusicPlaybackService : MediaSessionService` owns an ExoPlayer
(`REPEAT_MODE_ALL`, audio attributes for music, handle audio focus + becoming-noisy) wrapped in a
`ForwardingPlayer` whose `seekToPrevious()` goes to the previous media item (A-004) so the in-app button and the
notification's button behave the same. `PlayerRepository` is the domain seam: `playbackState: StateFlow<PlaybackState>`
(current song id/title/artist, isPlaying, hasQueue), `playQueue(songs, startIndex)`, `togglePlayPause()`,
`next()`, `previous()`, `connect()`/`release()`. `MusicViewModel` gains the playback actions and mirrors
`playbackState` into `MusicUiState`; `NowPlayingBar` shows title/artist and previous / play-pause / next.

## Acceptance

- DoD 8 via `MusicViewModelTest` over a fake `PlayerRepository`; DoD 9 via merged manifest; DoD 10/11 on device.
- Iteration wires: Media3 dependencies, `<service>` + FGS permissions, Koin binding, strings.

## Evidence

- Iteration 2: `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` → `BUILD SUCCESSFUL`; 34 tests
  (MusicViewModelTest 15), 0 failures; lint 0 errors / 63 warnings (new ones are dependency-version notices).
- Merged debug manifest: `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`, service
  `com.example.skeleton.data.service.MusicPlaybackService` `exported="true"` `foregroundServiceType="mediaPlayback"`
  with the `androidx.media3.session.MediaSessionService` intent-filter (DoD 9).
- Device `b56e2819`: install + `am start` → MainActivity resumed, 0 crash entries; `MusicPlaybackService` ServiceRecord
  bound by the controller. Audible playback not checked (no input injection) — DoD 10/11 stay `human`.

## Failed attempts

- Attempt 1 (build green, rejected by review): **B-1** Koin-`single` `PlayerRepositoryImpl` was released by the old
  `MusicViewModel.onCleared()` after Music → Home → Music (`toMusic` pops to graph), leaving the new screen with no
  listener — notification pause not reflected (DoD 11). **M-1** no `onDisconnected`, dead controller never replaced.
  **M-2** stale state after teardown. Minor: trailing content lambdas. No command failed; the reviewer's finding is the evidence.
- Attempt 2: ref-counted `connect()`/`release()`, `MediaController.Listener.onDisconnected` + `isConnected` check,
  `dropConnection()` resets to `PlaybackState.Idle`, explicit `content = {}`. Green.

## Notes

- T-002 left `onSongClick` as a no-op in `MusicFragment.kt` marked `// T-003 wires playback.`
- `formatDuration` is in `data/mapper/SongMapper.kt` (UI→data dependency, review minor). NowPlayingBar must not add a second UI import from `data.mapper`; moving it needs `SongRow.kt` and `SongMapperTest.kt` in scope — add them at dispatch, or leave it where it is.
