# T-003 - To-do: toggle done/not-done and delete a task

- **Status:** pending
- **Depends on:** T-002
- **Blocked by decision:** D-001
- **Attempts:** 0 / 3
- **Declared File Scope:**
  - `app/src/main/java/com/example/skeleton/domain/repository/TaskRepository.kt`
  - `app/src/main/java/com/example/skeleton/data/database/local/dao/TaskDao.kt`
  - `app/src/main/java/com/example/skeleton/data/repository/impl/TaskRepositoryImpl.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/todo/TodoViewModel.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/todo/component/TodoTaskItem.kt`
  - `app/src/test/java/com/example/skeleton/todo/FakeTaskRepository.kt`
  - `app/src/test/java/com/example/skeleton/todo/TaskToggleAndDeleteTest.kt`
- **Model Tier:** Fast <!-- bounded scope, extends the TaskRepository contract T-002 already fixed, no new
     architecture/dependency, acceptance is an exact pass/fail unit test -->

## Description

Add `suspend fun setDone(id: Long, done: Boolean)` and `suspend fun deleteTask(id: Long)` to
`TaskRepository` / `TaskDao` (`@Query` update + `@Delete` or `@Query("DELETE FROM tasks WHERE id = :id")`)
/ `TaskRepositoryImpl`. Add a checkbox (toggles done) and a delete affordance to `TodoTaskItem`, wired
through `TodoViewModel`. Done tasks render with strike-through / de-emphasised style. Extend
`FakeTaskRepository` with the same two operations.

## Acceptance

- Tapping a task's checkbox flips its done state in the list; tapping again flips it back.
- Deleting a task removes it from the list immediately.
- `TaskToggleAndDeleteTest` (fake repository): toggle done → not-done → done again; delete removes the
  task; deleting an unknown id is a no-op (no crash, list unchanged).
- Maps to DoD criteria 4, 13, 14.

## Evidence

## Failed attempts

## Notes
