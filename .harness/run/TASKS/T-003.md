# T-003 - Tapping the mini bar opens a Now Playing screen

- **Status:** complete (Phase 1, iteration 2; review follow-ups in T-005)
- **Depends on:** -
- **Blocked by decision:** - (D-001 answered)
- **Attempts:** 1 / 3
- **Declared File Scope:** (under `app/src/main/java/com/example/skeleton/`)
  `domain/model/NowPlaying.kt`, `domain/repository/MusicPlayerRepository.kt`,
  `data/repository/impl/MusicPlayerRepositoryImpl.kt`, `ui/fragment/music/MusicFragment.kt`,
  `ui/fragment/music/component/NowPlayingBar.kt`, new `ui/fragment/nowplaying/NowPlayingFragment.kt`,
  `ui/fragment/nowplaying/NowPlayingViewModel.kt`, `ui/fragment/nowplaying/NowPlayingUiState.kt`,
  `ui/fragment/nowplaying/component/*.kt` (including `NowPlayingArtwork.kt` with a placeholder body),
  `ui/fragment/nowplaying/model/PlaybackTime.kt`.
  Iteration writes from the report (C-11): `app/src/test/java/com/example/skeleton/ui/fragment/nowplaying/model/PlaybackTimeTest.kt`.
  Iteration wires: `navigation_graph.xml` (`nowPlayingFragment`, action `toNowPlaying`), `injection/ViewModelModule.kt`,
  both `strings.xml`, `README.md`, new drawables.
- **Model Tier:** Capable

## Description

- `NowPlaying` gains `durationMs: Long = 0L` (controller duration; `C.TIME_UNSET`/negative → 0).
- `MusicPlayerRepository` gains `fun currentPositionMs(): Long` (0 when not connected) and `fun seekTo(positionMs: Long)`
  (queued like other commands). Main thread only (C-07).
- `model/PlaybackTime.kt`: `formatPlaybackTime(ms: Long): String` ("m:ss", "h:mm:ss" from one hour, negative → "0:00"),
  `positionForFraction(fraction: Float, durationMs: Long): Long` (clamped), `fractionForPosition(positionMs, durationMs): Float`
  (0f for duration ≤ 0, clamped 0..1), `shouldTickPosition(isPlaying: Boolean, hasSong: Boolean): Boolean`.
- `NowPlayingBar` becomes tappable (`onClick`, with `onClickLabel`), its three buttons unchanged.
  `MusicFragment` passes `onNowPlayingClick = { safeNavigate(R.id.toNowPlaying) }`.
- `NowPlayingFragment` + `NowPlayingLayout(uiState, lambdas = {})`: `CoreLayout`, top bar with back, artwork slot
  (`NowPlayingArtwork(albumArtUri)` placeholder: music-note icon on `surfaceContainerHigh`), title + artist (marquee,
  C-06), M3 `Slider` (while dragging show the dragged value; `seekTo` on `onValueChangeFinished`), elapsed/total time,
  large M3 buttons (previous `IconButton`, big `FilledIconButton` play/pause, next). Previews for playing / paused /
  long title.
- `NowPlayingViewModel(musicPlayerRepository)`: own `connect()`/`release()` (C-08), collects `state`, polls
  `currentPositionMs()` ~500 ms while `shouldTickPosition`, forwards play/pause/next/previous/seek. When `currentSong`
  becomes null after having been set, the Fragment navigates back.

## Acceptance

- DoD #2: `PlaybackTimeTest` passes with the listed cases (plus `shouldTickPosition`).
- DoD #5: nav destination + action, Koin binding, `seekTo`, strings in both locales, README tree.
- Build + tests + lint green; human DoD #7–#10, #13.

## Evidence

- Iteration 2: `PlaybackTimeTest` 16/16 pass; nav `nowPlayingFragment` + `toNowPlaying` (action inside
  `musicFragment`), Koin `NowPlayingViewModel`, `seekTo` on the repository, 4 new keys in both locales, README tree
  updated; build + tests + lint green; install + start → `MusicFragment` on top, no app crash line.
- Worker wrote its own `NowPlayingTopBar` (CoreTopBar has hard-coded colours, C-01).

## Failed attempts

## Notes
