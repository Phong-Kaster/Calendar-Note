# T-007 - Calendar: edit and delete a note on the selected date

- **Status:** pending
- **Depends on:** T-006
- **Blocked by decision:** D-001
- **Attempts:** 0 / 3
- **Declared File Scope:**
  - `app/src/main/java/com/example/skeleton/domain/repository/NoteRepository.kt`
  - `app/src/main/java/com/example/skeleton/data/database/local/dao/NoteDao.kt`
  - `app/src/main/java/com/example/skeleton/data/repository/impl/NoteRepositoryImpl.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/CalendarUiState.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/CalendarViewModel.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/component/CalendarNoteList.kt`
  - `app/src/main/java/com/example/skeleton/ui/fragment/calendar/component/CalendarEditNoteDialog.kt`
  - `app/src/test/java/com/example/skeleton/calendar/NoteEditAndDeleteTest.kt`
- **Model Tier:** Capable <!-- new dialog + open-ended edit-mode UiState shape, same reasoning as T-004 -->

## Description

Add `suspend fun update(id: Long, title: String)` and `suspend fun delete(id: Long)` to `NoteRepository` /
`NoteDao` / `NoteRepositoryImpl`. Add edit and delete affordances to each row in `CalendarNoteList`, with
`CalendarEditNoteDialog` mirroring `TodoEditTaskDialog`'s pattern (T-004). Blank-title guard, same rule as
tasks and as note creation. This completes full CRUD for notes.

## Acceptance

- Editing a note on the selected date updates its title in that date's list only.
- Deleting a note removes it from that date's list; if it was the last note on that date, the date's
  "has notes" marker disappears.
- `NoteEditAndDeleteTest` (fake repository): edit changes the title, keeps the date; delete removes the
  note; delete of an unknown id is a no-op.
- Maps to DoD criteria 7, 22, 23 — and closes out the full note-CRUD requirement (DoD 6–7, 19–25 together).

## Evidence

## Failed attempts

## Notes
