{
  "description": "Goal-scoped Capability Ledger. Expires automatically when .harness/run/ is removed at completion.",
  "entries": [
    {
      "intent": "Add one testImplementation entry for kotlinx-coroutines-core IF the first gradlew.bat test run shows runBlocking/Flow do not resolve transitively on the JVM test classpath. Test source set only.",
      "command": "Edit app/build.gradle.kts and gradle/libs.versions.toml",
      "scope": "these two build files only; test source set dependency graph only; explicitly EXCLUDES kotlinx-coroutines-test, Robolectric, Turbine, MockK, and any androidTest dependency",
      "lifetime": "goal",
      "allow": [
        "Edit(app/build.gradle.kts)",
        "Edit(gradle/libs.versions.toml)"
      ]
    }
  ]
}
