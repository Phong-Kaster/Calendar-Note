# RESUME BLOCK

> Regenerated every iteration. The first thing a fresh iteration reads, and usually the only thing
> it needs before selecting work.
>
> **A derived cache, never a source of truth.** On any disagreement with the task files or with git,
> this file is the one that is wrong: correct it and trust the source. Recovery from a dirty tree
> always reads ground truth, never this.

- **Stage:** escalated
- **Next Phase:** none — every task is blocked by D-001 (DoD approval + standing capability grant)
- **Queued decisions:** 1 (D-001 - blocks T-001, T-002, T-003, T-004, T-005, T-006, T-007 - i.e. every task)
- **Abandoned:** none
- **Unreachable:** none
- **Verified commands:** build: unverified — proposed `gradlew.bat assembleDebug` | test: unverified — proposed `gradlew.bat test` | lint: unverified — proposed `gradlew.bat lint`
