{
  "description": "Standing Capability Ledger for this repository. Written by the human on approving escalation D-001 (2026-09-10). Standing = per-repository toolchain that survives this goal and future runs. The runtime reads only the 'allow' arrays, verbatim.",
  "entries": [
    {
      "intent": "Build, unit-test and lint the app so task completion can be evidenced (ENGINE.md §10). Nothing in this repository has been built by the engine yet; without this, no task can ever be marked complete. Includes the dependency resolution Gradle performs as part of these tasks.",
      "command": "./gradlew <task> (assemble / compile / unit test / lint only)",
      "scope": "consumer repository, module :app",
      "lifetime": "standing (per repository, survives this goal)",
      "allow": [
        "Bash(./gradlew --version)",
        "Bash(./gradlew :app:assembleDebug*)",
        "Bash(./gradlew :app:compileDebugKotlin*)",
        "Bash(./gradlew :app:testDebugUnitTest*)",
        "Bash(./gradlew :app:lintDebug*)"
      ]
    },
    {
      "intent": "Run and refresh host-side Compose Preview Screenshot Tests. Granted as part of Q4 Option B+: this repository already carries a verified screenshot harness on the branch loop/todo-calendar-screens, and these are the two Gradle tasks that drive it. Host-side (layoutlib on the JVM) - no emulator, no device, no adb.",
      "command": "./gradlew :app:validateDebugScreenshotTest / :app:updateDebugScreenshotTest",
      "scope": "consumer repository, module :app",
      "lifetime": "standing (per repository, survives this goal)",
      "allow": [
        "Bash(./gradlew :app:validateDebugScreenshotTest*)",
        "Bash(./gradlew :app:updateDebugScreenshotTest*)"
      ]
    },
    {
      "intent": "Read files out of earlier commits and out of a prior run's Loop Branch. Bash(git show*) is already baseline, but on Windows/Git Bash MSYS rewrites 'ref:path' into 'ref;path' and the command fails for exactly the paths that matter (.ai/**, knowledge/**). MSYS_NO_PATHCONV=1 is the documented workaround, and the leading assignment means the baseline rule does not match it.",
      "command": "MSYS_NO_PATHCONV=1 git show <ref>:<path>",
      "scope": "consumer repository, read-only",
      "lifetime": "standing (per repository, survives this goal)",
      "allow": [
        "Bash(MSYS_NO_PATHCONV=1 git show*)",
        "Bash(MSYS_NO_PATHCONV=1 git ls-tree*)"
      ]
    }
  ]
}
